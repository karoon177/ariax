# استقرار AriaX روی Render

## پیش‌نیاز
- حساب Render با ایمیل مالک
- یک مخزن خصوصی GitHub/GitLab که محتوای پوشه `exchange/` در ریشهٔ آن قرار گرفته باشد

## مراحل در Render
1. Dashboard → **New** → **Blueprint** را انتخاب کنید.
2. حساب GitHub/GitLab و مخزن را متصل کنید.
3. Render فایل `render.yaml` را می‌خواند. نام سرویس `ariax-testnet` است.
4. پلن **Free** را انتخاب کنید. این انتخاب برای تست‌نت مناسب است، اما سرویس پس از مدتی بی‌استفاده‌بودن ممکن است sleep شود و اولین درخواست بعدی با تأخیر بیدارشدن پاسخ می‌گیرد؛ بنابراین تضمین «همیشه آنلاین» ندارد.
5. پس از Deploy، مسیر `/api/markets` باید در Health Check سبز شود.
6. در صورت نیاز، از Settings → Custom Domains دامنهٔ خود را متصل کنید.

## داده‌ها
در نسخهٔ فعلی SQLite برای تست‌نت استفاده شده است. در پلن رایگان، ماندگاری داده‌ها پس از redeploy تضمین نیست. برای ماندگاری کامل در ارتقای آینده، Persistent Disk یا PostgreSQL اضافه می‌شود.

## امنیت
هیچ رمز عبور Render، GitHub، Cloudflare یا ایمیل را در چت ارسال نکنید. اگر رمزی منتشر شده، آن را فوراً تغییر دهید.
